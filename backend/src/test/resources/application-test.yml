spring:
  datasource:
    url: jdbc:h2:mem:monitoramento_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false

app:
  jwt:
    secret: segredo-de-teste-com-no-minimo-32-caracteres-1234
    expiration-ms: 3600000
